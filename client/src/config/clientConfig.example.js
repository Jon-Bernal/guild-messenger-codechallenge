export const baseURL = "http://example.com/api"; // use https if in production evironment
export const baseWebsocketURL = "ws://127.0.0.1:5000/api"; // wss if in production environment
export const secret = "some large random string";
